---
description: Turn a settled idea into a proposal, design, tasks, and spec artifacts
argument-hint: "<change-name>"
---

# Propose

Follow the sdd `propose` skill (load `skills/propose/SKILL.md` in the pi-sdd package and follow it) for the user's request below.

User request: $@
