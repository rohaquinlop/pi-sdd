---
description: Implement a change's tasks.md against real code, checking tasks off as they complete
argument-hint: "[change-name]"
---

# Apply

Follow the sdd `apply` skill (load `skills/apply/SKILL.md` in the pi-sdd package and follow it) for the user's request below.

User request: $@
